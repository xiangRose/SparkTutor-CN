- Class: meta
  Lesson: 简介与快速上手
  EstimatedMinutes: 25

- Class: text
  Depth: all
  Output: |
    ## 你将学到什么

    学完本课程后，你将能够：

    - 解释什么是 Apache Spark 以及它为什么会出现
    - 描述 Driver-Executor 架构
    - 创建 SparkSession —— 所有 Spark 功能的入口
    - 将 CSV 文件读取为 DataFrame
    - 使用 `groupBy`、`agg` 和 `orderBy` 进行基本聚合
    - 过滤结果以回答关于数据的具体问题

    本课程是后续所有内容的基础。每个 Spark 程序都从
    SparkSession 开始，并围绕 DataFrame 展开。

- Class: text
  Depth: all
  Output: |
    ## 什么是 Apache Spark？

    Apache Spark 是一个面向大规模数据处理的**统一分析引擎**。
    它通过单一框架处理 SQL 查询、流数据、机器学习和图计算。

    Spark 的核心创新是**内存计算**。早期的 MapReduce 模型在每一步
    都将中间结果写入磁盘。Spark 在操作之间将数据保留在内存中，
    这使得迭代式工作负载（如机器学习训练）速度提升 10-100 倍。

    Spark 提供 Python、Scala、Java 和 R 的 API。本课程全程使用
    PySpark —— 即 Python API。在底层，PySpark 与同一个基于 JVM 的
    引擎通信，因此你在编写 Python 的同时能获得完整的 Spark 性能。

- Class: text
  Depth: beginner
  Output: |
    ## 类比：把 Spark 想象成餐厅后厨

    把 Spark 想象成一个繁忙的餐厅后厨：

    - **Driver（驱动程序）** 是**主厨** —— 它读取订单（你的代码），
      规划步骤，并协调谁做什么。它自己不做菜。
    - **Executor（执行器）** 是**后厨厨师** —— 它们在分配到的数据
      分片上做实际工作：切配、烹饪、摆盘。
    - **Cluster Manager（集群管理器）** 是**餐厅经理** —— 它决定
      有多少厨师可用并分配资源。

    当你提交一个 Spark 作业时，主厨（Driver）将其拆分成多个任务，
    交给后厨厨师（Executor），他们并行工作以快速产出结果。
    厨师越多 = 并行度越高 = 结果越快。

- Class: text
  Depth: all
  Output: |
    ## Spark 架构

    一个 Spark 应用有四个核心组件：

    - **Driver Program（驱动程序）** —— 运行你的 `main()` 函数，
      创建 SparkSession，并协调整个集群的执行。
    - **Cluster Manager（集群管理器）** —— 分配资源。可选方案包括
      Standalone、YARN、Mesos 和 Kubernetes。
    - **Worker Nodes（工作节点）** —— 集群中托管 Executor 的机器。
    - **Executors（执行器）** —— Worker 上运行任务并缓存数据的
      JVM 进程。

    执行层级：

    ```
    Application  →  Jobs  →  Stages  →  Tasks
    ```

    一个 **job** 由一个 action 触发（例如 `show()`、`count()`）。
    Job 在 shuffle 边界处被划分为多个 **stage**。Stage 包含多个
    **task** —— 每个数据分区一个 —— 它们在各个 executor 上并行运行。

- Class: text
  Depth: intermediate
  Output: |
    ## SparkSession：统一的入口

    从 Spark 2.0 开始，`SparkSession` 取代了旧的 SparkContext、
    SQLContext 和 HiveContext。你通过**建造者模式**创建它：

    ```python
    from pyspark.sql import SparkSession

    spark = (SparkSession.builder
        .appName("MyApp")
        .master("local[*]")
        .config("spark.some.option", "value")
        .getOrCreate())
    ```

    `master` 设置控制 Spark 在哪里运行：
    - `local[*]` —— 使用所有本地 CPU 核心（适合开发）
    - `local[4]` —— 恰好使用 4 个核心
    - `spark://host:7077` —— 连接到 Standalone 集群
    - `k8s://...` —— 连接到 Kubernetes 集群

    `getOrCreate()` 会创建新会话或返回已存在的会话，因此多次调用
    是安全的。

    **文档：** [SparkSession](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.SparkSession.html)
    —— 建造者模式、`appName()`、`master()`、`config()`、`getOrCreate()`。

- Class: mult_question
  Depth: all
  Output: "Driver 在 Spark 应用中的作用是什么？"
  AnswerChoices: "协调执行并规划查询;将数据存储在磁盘上;在数据分区上执行单个任务;管理集群资源"
  CorrectAnswer: "协调执行并规划查询"
  Hint: "想想主厨的类比 —— 谁规划工作但不亲自做菜？"

- Class: text
  Depth: advanced
  Output: |
    ## 执行模型详解

    当你调用一个 action 时，Spark 的执行引擎启动：

    1. **DAG 调度器** —— 分析转换链并构建有向无环图（DAG）。
       它在 **shuffle 边界**处将 DAG 拆分为多个 stage —— 即数据必须
       在分区之间重新分布的点。

    2. **任务调度器** —— 为每个 stage 创建任务（每个分区一个），
       然后将它们分配给可用的 executor。

    **窄依赖与宽依赖：**
    - **窄依赖**（例如 `map`、`filter`、`select`）—— 每个输入分区
      只贡献到一个输出分区。没有数据移动。
    - **宽依赖**（例如 `groupBy`、`join`、`repartition`）—— 输入分区
      贡献到多个输出分区。需要 **shuffle** —— 代价高昂的网络 I/O。

    **惰性求值：**
    转换会累积成逻辑计划。只有 action 才会触发执行。
    这让 Spark 的 Catalyst 优化器能在生成代码之前分析整个管道并
    重写它 —— 谓词下推、列裁剪、join 重排序 —— 全部透明发生。

- Class: cmd_question
  Depth: all
  KnowledgeComponents: [spark.session.builder]
  Output: |
    创建一个 SparkSession，应用名称设为 `'MnMCount'`，master 设为
    `'local[*]'`。将其存储在名为 `spark` 的变量中。
  CorrectAnswer: |
    from pyspark.sql import SparkSession

    spark = SparkSession.builder.appName("MnMCount").master("local[*]").getOrCreate()
  Hint: "使用 SparkSession.builder，链式调用 .appName() 和 .master()，然后调用 .getOrCreate()。"

- Class: mult_question
  Depth: all
  Output: "以下哪个会在 Spark 中触发实际计算（是 action，而非 transformation）？"
  AnswerChoices: "show();select();filter();withColumn()"
  CorrectAnswer: "show()"
  Hint: "Transformation 是惰性的 —— 它们只构建计划。Action 会触发计划执行并产生结果。"

- Class: text
  Depth: all
  Output: |
    ## 读取数据

    Spark 通过 **DataFrameReader** API 读取数据。最常见的模式：

    ```python
    df = (spark.read
        .format("csv")
        .option("header", "true")
        .option("inferSchema", "true")
        .load("/path/to/file.csv"))
    ```

    或者使用简写：

    ```python
    df = spark.read.csv("/path/to/file.csv", header=True, inferSchema=True)
    ```

    这会得到一个 **DataFrame** —— 一张具有命名类型化列的分布式表。
    可以把它想象成一张能横跨数千台机器的电子表格。

    **文档：** [DataFrameReader.csv](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrameReader.csv.html)
    —— `header`、`inferSchema`、`schema`、`sep`、`nullValue` 等全部参数。

- Class: text
  Depth: all
  Output: |
    ## 聚合与过滤

    一旦有了 DataFrame，就可以用转换来分析数据：

    ```python
    from pyspark.sql import functions as f

    result = (df
        .groupBy("State", "Color")
        .agg(f.sum("Count").alias("Total"))
        .orderBy(f.col("Total").desc()))
    ```

    逐一拆解：
    - `groupBy("State", "Color")` —— 按唯一组合对行分组
    - `agg(f.sum("Count").alias("Total"))` —— 对 Count 列求和，
      命名为 "Total"
    - `orderBy(f.col("Total").desc())` —— 按 Total 降序排列

    `filter()` 和 `where()` 选择满足条件的行：

    ```python
    ca_only = result.filter(f.col("State") == "CA")
    ```

    **文档：**
    [f.col](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.functions.col.html) |
    [f.sum](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.functions.sum.html) |
    [DataFrame.groupBy](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrame.groupBy.html) |
    [Column.alias](https://spark.apache.org/docs/3.5.0/api/python/reference/pyspark.sql/api/pyspark.sql.Column.alias.html)

- Class: cmd_question
  Depth: intermediate
  KnowledgeComponents: [dataframe.groupby, dataframe.aggregation, dataframe.orderby]
  Output: |
    给定一个包含 `State`、`Color` 和 `Count` 列的 DataFrame `df`，
    编写代码按 State 和 Color 分组，对 Count 列求和（别名为 "Total"），
    并按总和降序排列。
  CorrectAnswer: |
    from pyspark.sql import functions as f

    result = (df.groupBy("State", "Color")
        .agg(f.sum("Count").alias("Total"))
        .orderBy(f.col("Total").desc()))
  Hint: "groupBy 传两列，agg 用 f.sum() 和 alias()，然后 orderBy 用 desc()。"

- Class: text
  Depth: intermediate
  Output: |
    ## 读取数据：选项与格式

    `option()` 方法配置 Spark 如何读取文件。对于 CSV：

    - `header` —— 第一行是否为列名（true/false）
    - `inferSchema` —— 是否扫描文件来检测类型（true/false）
    - `sep` —— 字段分隔符（默认逗号）
    - `nullValue` —— 视为 null 的字符串

    CSV 之外的常见格式：**JSON**（半结构化）、**Parquet**
    （列式存储、压缩 —— Spark 首选格式）和 **ORC**（与 Parquet 类似，
    在 Hive 生态中流行）。

    我们将在下一节关于数据源的课程中深入探讨所有这些格式。

- Class: text
  Depth: advanced
  Output: |
    ## 为什么 inferSchema 有代价

    当你对 CSV 或 JSON 设置 `inferSchema=true` 时，Spark 会对数据
    做**两遍**扫描：一遍采样行并推断类型，另一遍实际加载数据。
    对于大文件，这会使读取时间翻倍。

    在生产环境中，始终用 `StructType` 显式定义 schema。你将在下一节
    课程中学到如何做。目前，`inferSchema` 适合探索和小数据集。

- Class: script
  Depth: all
  KnowledgeComponents: [spark.read.csv, dataframe.groupby, dataframe.aggregation, dataframe.filter]
  Transfer: true
  SourceTask: getting_started:guided_operations
  TransferDistance: structural
  Output: |
    ## 练习：M&M 糖果计数分析

    是时候将所有内容综合运用了。实现 `mnm_analysis` 函数，
    改编自《Learning Spark》一书中的 M&M 计数示例。

    你的函数应该：
    1. 读取一个 CSV 文件（带 header 和 inferSchema），包含列：
       `State`、`Color`、`Count`
    2. 按 State 和 Color 分组，对 Count 列求和（别名为 "Total"）
    3. 按 Total 降序排列
    4. 仅筛选加利福尼亚州（`'CA'`）
    5. 返回仅包含加利福尼亚州的 DataFrame

    这道练习综合运用了本课的所有技能：SparkSession、读取数据、
    groupBy、agg、orderBy 和 filter。
  StarterFile: starter.py
  SolutionFile: solution.py
  Hint: "用 spark.read.csv() 和选项读取。groupBy 两列，agg 用 f.sum()，orderBy desc，然后 filter State == 'CA'。"

- Class: text
  Depth: all
  Output: |
    ## 小结

    本课你学到了：

    - **什么是 Spark** —— 一个具有内存计算能力的统一分析引擎
    - **Spark 架构** —— Driver 负责协调，Executor 做实际工作，
      Cluster Manager 分配资源
    - **SparkSession** —— 入口点，通过建造者模式创建
    - **读取数据** —— `spark.read.csv()` 以及 header、
      inferSchema 等选项
    - **聚合** —— `groupBy`、`agg`、`orderBy` 用于数据分析
    - **过滤** —— `filter()` 缩小结果范围

    你编写了第一个完整的 Spark 程序。在下一课中，你将学习
    DataFrame 在底层如何工作 —— schema、列表达式和让 Spark
    在大规模下可靠运行的类型系统。
